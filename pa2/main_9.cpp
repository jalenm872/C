#include <iostream>

//

int main(){
    int a,b,c;
    std::cin >> a >> b >> c;
    if ( a < 0 && b < 0 && c < 0 && a <= 20 && b <= 20 && c <= 20 && a < b ){
        //Rowss
        for(int i = 0; i < (b-a)-1; i++){
            for(int j = 0; i <= b-1; i++)
        }
    }
}


