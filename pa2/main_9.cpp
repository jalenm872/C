#include <iostream>

//Draw Triangle 3

int main(){
    //Variables
    int a,b,c;
    //User Input
    std::cin >> a >> b >> c;
    //If statment for prerequisites
    if ( a < 0 && b < 0 && c < 0 && a <= 20 && b <= 20 && c <= 20 && a < b ){
        //Rowss
        for(int i = 0; i < (b-a)-1; i++){
            //Columns
            for(int j = 0; i <= b-1; i++)
        }
    }
}


